{
  "name": "my-test-app",
  "version": "1.0.0",
  "description": "This project contains source code and supporting files for a serverless application that you can deploy with the AWS Serverless Application Model (AWS SAM) command line interface (CLI). It includes the following files and folders:",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pre-build": "find ./src ! -path '*/node_modules/*' -name 'package.json' -execdir npm install \\;",
    "build": "tsc -p ./src && sam build -s ./src && find .aws-sam -name '*.ts' -type f|xargs rm -f && find src ! -path '*/node_modules/*' -name '*.js' -type f|xargs rm -f",
    "deploy-env": "sam deploy --config-env dev --parameter-overrides StageName=dev",
    "format": "npx prettier --write ."
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.83",
    "@types/node": "^16.10.2",
    "@typescript-eslint/eslint-plugin": "^4.32.0",
    "@typescript-eslint/parser": "^4.32.0",
    "aws-sdk": "^2.999.0",
    "prettier": "2.4.1"
  }
}
